schema_version = "1.0.0"

id = "yl_cameraref"
version = "1.0.0"
name = "YL CameraRef"
tagline = "Camera reference image tools"
maintainer = "YLIANG"
type = "add-on"

license = ["SPDX:GPL-3.0-or-later"]
blender_version_min = "4.2.0"
tags = ["3D View", "Camera"]

[permissions]
files = "Load reference images selected by the user"
